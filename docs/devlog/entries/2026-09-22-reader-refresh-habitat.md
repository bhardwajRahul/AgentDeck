# 2026-09-22 — Crema native refresh and static reader habitat

- Crema S optical feedback: prior animation stuttered/ghosted; after the first patch the user reported acceptable ghosting with some flicker and accepted leaving mild flicker. Panel behavior cannot be established from ADB screenshots.
- Inspected the connected CREMAA21W09235 firmware: IWG / Qualcomm sdm660, Android 10, with native Onyx `android.view.View` extensions. Corrected the deployment registry and hardware/UI docs that called it RK3566.
- Removed the unsupported `sys.eink.update` property fallback, which emitted permission-denied warnings every animation frame. Probe native Onyx methods once, set default waveform on the zone and its descendants, and use ordinary `invalidate()`; reserve explicit `refreshScreen(...)` for full/attention requests. Do not lock a global waveform. A first probe using `invalidate(int)` was denied by Android's hidden-API blacklist; the final installed build uses the permitted methods and logs `Using native Onyx View regional refresh` (Crema PID 4966).
- Aquarium state transitions now request one debounced regional grayscale cleanup rather than a full flash both immediately and 500 ms later. Attention's existing full-once behavior remains separate. Physical aquarium cadence stays capped at 10 Hz; LCD uses vsync.
- Enabled the existing Blender-authored static habitat on physical readers. Monochrome EPD backgrounds are lifted and quantized once to fixed 16-level gray; no temporal dithering. Added bounded edge-plant sway and a grazing snail; small bubbles are LCD/color only. Blender MCP confirmed the retained habitat scene; no changes to the `.blend` asset were needed.
- Sideload build 1.4.1/code19 installed on Crema, Pantone and Lenovo. This is device validation, not a public release. The final native-API correction followed the user's optical feedback, so remaining flicker on that exact final build is not independently optically measured.
- Validation: Android 404 tests passed; workspace build/typecheck passed. Workspace suite had 4670 pass/2 skip plus one live-loopback port-race failure in `esp32-suspend-sweep`; that file passed all 7 tests when rerun alone. Release APK build passed. Protocol generation had no drift; tokens and documentation/catalog/devlog checks passed, deploy skill validation passed, and design lint stayed at the prior 92 (89 tracked baseline + 3 ignored artifacts). Screenshots stay local; crop only the aquarium when sharing because timelines can contain private user data.
