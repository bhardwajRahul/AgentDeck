# 2026-09-24 — IPS10 USAGE, collaboration and voice feedback

The IPS10 workspace now groups confirmed provider plans, subscription dates and actual quota windows under USAGE. Six fixed circular gauges serve Claude, Codex and z.ai; Antigravity remains plan/date-only. Missing windows remain absent. Luna becomes the Codex reading only while a regular window is exhausted, with a crescent and remaining percentage; a reset restores regular windows. Four subscription slots avoid dropping the fourth provider. Shared TypeScript policy generates the native usage predicate/labels and Collaboration census labels/phase; Stream Deck and Ulanzi use the same Luna selector. Android e-ink headings now say USAGE.

Aquarium/Details is a graphical segmented control. Removed the Connected, OpenClaw ready and AGY header labels and token-counter footer. The bottom strip reports wake availability, capture/transcription/processing/speaking, recognized words, owner and reply. Fixed buffers carry audio/network updates into the UI task. Details uses macOS Collaboration's four live census metrics and waiting-on-work semantics; it does not claim the device's bounded recent events are the full macOS task history. Portrait and landscape content are bounded above the voice strip.

The reported symptom is successful wake activation followed by incorrect speech recognition. Runtime inspection found an old global npm daemon executing Apple Speech directly despite local settings selecting whisper-cpp / Korean / large-v3-turbo. Source already contains the configured backend and personal OpenClaw route. A health diagnostic now reports the configured voice backend and route, without claiming backend readiness. Replaying the previously captured microphone WAV through Whisper still misrecognized part of the sentence, so switching the running implementation alone is not proof of recognition accuracy.

Camera review confirms only the OV02C10 SCCB sensor-ID probe. IPS10 CSI/ISP capture and coexistence with display/audio remain unimplemented; no camera or vision feature is presented as operational.

Validation: native IPS10 interaction checks pass in landscape and portrait, including provider/window absence, plan-only metadata, Luna transition/reset, collaboration metrics, voice transcript/reply and roster pagination. IPS35 native build/render passes. macOS and Android release builds pass. Protocol generation and generated display-policy mirrors have no drift; token mirrors pass. Design lint retains 171 existing source/build-artifact findings, with no changed HTML/CSS/JS source in its scope. Deployment evidence is recorded after runtime verification.
